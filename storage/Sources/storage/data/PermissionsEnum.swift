// PermissionsEnum enumerates the values for permissions enum.

public enum PermissionsEnum: String, Codable
{
// A specifies the a state for permissions enum.
    case A = "a"
// C specifies the c state for permissions enum.
    case C = "c"
// D specifies the d state for permissions enum.
    case D = "d"
// L specifies the l state for permissions enum.
    case L = "l"
// P specifies the p state for permissions enum.
    case P = "p"
// R specifies the r state for permissions enum.
    case R = "r"
// U specifies the u state for permissions enum.
    case U = "u"
// W specifies the w state for permissions enum.
    case W = "w"
}
