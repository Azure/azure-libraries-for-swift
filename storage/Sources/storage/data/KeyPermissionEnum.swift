// KeyPermissionEnum enumerates the values for key permission enum.

public enum KeyPermissionEnum: String, Codable
{
// Full specifies the full state for key permission enum.
    case Full = "Full"
// Read specifies the read state for key permission enum.
    case Read = "Read"
}
