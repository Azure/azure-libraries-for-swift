// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License. See License.txt in the project root for license information.
//
// Code generated by Microsoft (R) AutoRest Code Generator.
import Foundation
import azureSwiftRuntime
internal struct StorageAccountUpdateParametersData : StorageAccountUpdateParametersProtocol {
    public var sku: SkuProtocol?
    public var tags: [String:String]?
    public var identity: IdentityProtocol?
    public var properties: StorageAccountPropertiesUpdateParametersProtocol?
    public var kind: KindEnum?

        enum CodingKeys: String, CodingKey {case sku = "sku"
        case tags = "tags"
        case identity = "identity"
        case properties = "properties"
        case kind = "kind"
        }

  public init()  {
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)
      if container.contains(.sku) {
        self.sku = try container.decode(SkuData?.self, forKey: .sku)
    }
    if container.contains(.tags) {
        self.tags = try container.decode([String:String]?.self, forKey: .tags)
    }
    if container.contains(.identity) {
        self.identity = try container.decode(IdentityData?.self, forKey: .identity)
    }
    if container.contains(.properties) {
        self.properties = try container.decode(StorageAccountPropertiesUpdateParametersData?.self, forKey: .properties)
    }
    if container.contains(.kind) {
        self.kind = try container.decode(KindEnum?.self, forKey: .kind)
    }
    if var pageDecoder = decoder as? PageDecoder  {
      if pageDecoder.isPagedData,
        let nextLinkName = pageDecoder.nextLinkName {
          pageDecoder.nextLink = try UnknownCodingKey.decodeStringForKey(decoder: decoder, keyForDecode: nextLinkName)
      }
    }
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)
    if self.sku != nil {try container.encode(self.sku as! SkuData?, forKey: .sku)}
    if self.tags != nil {try container.encode(self.tags, forKey: .tags)}
    if self.identity != nil {try container.encode(self.identity as! IdentityData?, forKey: .identity)}
    if self.properties != nil {try container.encode(self.properties as! StorageAccountPropertiesUpdateParametersData?, forKey: .properties)}
    if self.kind != nil {try container.encode(self.kind, forKey: .kind)}
  }
}

extension DataFactory {
  public static func createStorageAccountUpdateParametersProtocol() -> StorageAccountUpdateParametersProtocol {
    return StorageAccountUpdateParametersData()
  }
}
