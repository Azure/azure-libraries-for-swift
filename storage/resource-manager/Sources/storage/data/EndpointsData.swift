// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License. See License.txt in the project root for license information.
//
// Code generated by Microsoft (R) AutoRest Code Generator.
import Foundation
import azureSwiftRuntime
internal struct EndpointsData : EndpointsProtocol {
    public var blob: String?
    public var queue: String?
    public var table: String?
    public var file: String?

        enum CodingKeys: String, CodingKey {case blob = "blob"
        case queue = "queue"
        case table = "table"
        case file = "file"
        }

  public init()  {
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)
      if container.contains(.blob) {
        self.blob = try container.decode(String?.self, forKey: .blob)
    }
    if container.contains(.queue) {
        self.queue = try container.decode(String?.self, forKey: .queue)
    }
    if container.contains(.table) {
        self.table = try container.decode(String?.self, forKey: .table)
    }
    if container.contains(.file) {
        self.file = try container.decode(String?.self, forKey: .file)
    }
    if var pageDecoder = decoder as? PageDecoder  {
      if pageDecoder.isPagedData,
        let nextLinkName = pageDecoder.nextLinkName {
          pageDecoder.nextLink = try UnknownCodingKey.decodeStringForKey(decoder: decoder, keyForDecode: nextLinkName)
      }
    }
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)
    if self.blob != nil {try container.encode(self.blob, forKey: .blob)}
    if self.queue != nil {try container.encode(self.queue, forKey: .queue)}
    if self.table != nil {try container.encode(self.table, forKey: .table)}
    if self.file != nil {try container.encode(self.file, forKey: .file)}
  }
}

extension DataFactory {
  public static func createEndpointsProtocol() -> EndpointsProtocol {
    return EndpointsData()
  }
}
