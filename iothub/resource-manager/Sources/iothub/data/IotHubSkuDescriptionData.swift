// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License. See License.txt in the project root for license information.
//
// Code generated by Microsoft (R) AutoRest Code Generator.
import Foundation
import azureSwiftRuntime
internal struct IotHubSkuDescriptionData : IotHubSkuDescriptionProtocol {
    public var resourceType: String?
    public var sku: IotHubSkuInfoProtocol
    public var capacity: IotHubCapacityProtocol

        enum CodingKeys: String, CodingKey {case resourceType = "resourceType"
        case sku = "sku"
        case capacity = "capacity"
        }

  public init(sku: IotHubSkuInfoProtocol, capacity: IotHubCapacityProtocol)  {
    self.sku = sku
    self.capacity = capacity
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)
      if container.contains(.resourceType) {
        self.resourceType = try container.decode(String?.self, forKey: .resourceType)
    }
    self.sku = try container.decode(IotHubSkuInfoData.self, forKey: .sku)
    self.capacity = try container.decode(IotHubCapacityData.self, forKey: .capacity)
    if var pageDecoder = decoder as? PageDecoder  {
      if pageDecoder.isPagedData,
        let nextLinkName = pageDecoder.nextLinkName {
          pageDecoder.nextLink = try UnknownCodingKey.decodeStringForKey(decoder: decoder, keyForDecode: nextLinkName)
      }
    }
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)
    if self.resourceType != nil {try container.encode(self.resourceType, forKey: .resourceType)}
    try container.encode(self.sku as! IotHubSkuInfoData, forKey: .sku)
    try container.encode(self.capacity as! IotHubCapacityData, forKey: .capacity)
  }
}

extension DataFactory {
  public static func createIotHubSkuDescriptionProtocol(sku: IotHubSkuInfoProtocol, capacity: IotHubCapacityProtocol) -> IotHubSkuDescriptionProtocol {
    return IotHubSkuDescriptionData(sku: sku, capacity: capacity)
  }
}
