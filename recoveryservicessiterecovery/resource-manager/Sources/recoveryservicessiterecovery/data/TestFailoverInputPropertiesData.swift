// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License. See License.txt in the project root for license information.
//
// Code generated by Microsoft (R) AutoRest Code Generator.
import Foundation
import azureSwiftRuntime
internal struct TestFailoverInputPropertiesData : TestFailoverInputPropertiesProtocol {
    public var failoverDirection: String?
    public var networkType: String?
    public var networkId: String?
    public var skipTestFailoverCleanup: String?
    public var providerSpecificDetails: ProviderSpecificFailoverInputProtocol?

        enum CodingKeys: String, CodingKey {case failoverDirection = "failoverDirection"
        case networkType = "networkType"
        case networkId = "networkId"
        case skipTestFailoverCleanup = "skipTestFailoverCleanup"
        case providerSpecificDetails = "providerSpecificDetails"
        }

  public init()  {
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)
      if container.contains(.failoverDirection) {
        self.failoverDirection = try container.decode(String?.self, forKey: .failoverDirection)
    }
    if container.contains(.networkType) {
        self.networkType = try container.decode(String?.self, forKey: .networkType)
    }
    if container.contains(.networkId) {
        self.networkId = try container.decode(String?.self, forKey: .networkId)
    }
    if container.contains(.skipTestFailoverCleanup) {
        self.skipTestFailoverCleanup = try container.decode(String?.self, forKey: .skipTestFailoverCleanup)
    }
    if container.contains(.providerSpecificDetails) {
        self.providerSpecificDetails = try container.decode(ProviderSpecificFailoverInputData?.self, forKey: .providerSpecificDetails)
    }
    if var pageDecoder = decoder as? PageDecoder  {
      if pageDecoder.isPagedData,
        let nextLinkName = pageDecoder.nextLinkName {
          pageDecoder.nextLink = try UnknownCodingKey.decodeStringForKey(decoder: decoder, keyForDecode: nextLinkName)
      }
    }
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)
    if self.failoverDirection != nil {try container.encode(self.failoverDirection, forKey: .failoverDirection)}
    if self.networkType != nil {try container.encode(self.networkType, forKey: .networkType)}
    if self.networkId != nil {try container.encode(self.networkId, forKey: .networkId)}
    if self.skipTestFailoverCleanup != nil {try container.encode(self.skipTestFailoverCleanup, forKey: .skipTestFailoverCleanup)}
    if self.providerSpecificDetails != nil {try container.encode(self.providerSpecificDetails as! ProviderSpecificFailoverInputData?, forKey: .providerSpecificDetails)}
  }
}

extension DataFactory {
  public static func createTestFailoverInputPropertiesProtocol() -> TestFailoverInputPropertiesProtocol {
    return TestFailoverInputPropertiesData()
  }
}
