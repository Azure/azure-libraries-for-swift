// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License. See License.txt in the project root for license information.
//
// Code generated by Microsoft (R) AutoRest Code Generator.
import Foundation
import azureSwiftRuntime
internal struct CurrentScenarioDetailsData : CurrentScenarioDetailsProtocol {
    public var scenarioName: String?
    public var jobId: String?
    public var startTime: Date?

        enum CodingKeys: String, CodingKey {case scenarioName = "scenarioName"
        case jobId = "jobId"
        case startTime = "startTime"
        }

  public init()  {
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)
      if container.contains(.scenarioName) {
        self.scenarioName = try container.decode(String?.self, forKey: .scenarioName)
    }
    if container.contains(.jobId) {
        self.jobId = try container.decode(String?.self, forKey: .jobId)
    }
    if container.contains(.startTime) {
        self.startTime = DateConverter.fromString(dateStr: (try container.decode(String?.self, forKey: .startTime)), format: .dateTime)
    }
    if var pageDecoder = decoder as? PageDecoder  {
      if pageDecoder.isPagedData,
        let nextLinkName = pageDecoder.nextLinkName {
          pageDecoder.nextLink = try UnknownCodingKey.decodeStringForKey(decoder: decoder, keyForDecode: nextLinkName)
      }
    }
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)
    if self.scenarioName != nil {try container.encode(self.scenarioName, forKey: .scenarioName)}
    if self.jobId != nil {try container.encode(self.jobId, forKey: .jobId)}
    if self.startTime != nil {
        try container.encode(DateConverter.toString(date: self.startTime!, format: .dateTime), forKey: .startTime)
    }
  }
}

extension DataFactory {
  public static func createCurrentScenarioDetailsProtocol() -> CurrentScenarioDetailsProtocol {
    return CurrentScenarioDetailsData()
  }
}
