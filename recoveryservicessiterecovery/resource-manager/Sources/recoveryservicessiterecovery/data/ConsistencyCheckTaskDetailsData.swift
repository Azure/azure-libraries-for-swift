// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License. See License.txt in the project root for license information.
//
// Code generated by Microsoft (R) AutoRest Code Generator.
import Foundation
import azureSwiftRuntime
internal struct ConsistencyCheckTaskDetailsData : ConsistencyCheckTaskDetailsProtocol, TaskTypeDetailsProtocol {
    public var vmDetails: [InconsistentVmDetailsProtocol?]?

        enum CodingKeys: String, CodingKey {case vmDetails = "vmDetails"
        }

  public init()  {
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)
      if container.contains(.vmDetails) {
        self.vmDetails = try container.decode([InconsistentVmDetailsData?]?.self, forKey: .vmDetails)
    }
    if var pageDecoder = decoder as? PageDecoder  {
      if pageDecoder.isPagedData,
        let nextLinkName = pageDecoder.nextLinkName {
          pageDecoder.nextLink = try UnknownCodingKey.decodeStringForKey(decoder: decoder, keyForDecode: nextLinkName)
      }
    }
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)
    if self.vmDetails != nil {try container.encode(self.vmDetails as! [InconsistentVmDetailsData?]?, forKey: .vmDetails)}
  }
}

extension DataFactory {
  public static func createConsistencyCheckTaskDetailsProtocol() -> ConsistencyCheckTaskDetailsProtocol {
    return ConsistencyCheckTaskDetailsData()
  }
}
