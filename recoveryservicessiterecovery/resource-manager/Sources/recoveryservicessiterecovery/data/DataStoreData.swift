// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License. See License.txt in the project root for license information.
//
// Code generated by Microsoft (R) AutoRest Code Generator.
import Foundation
import azureSwiftRuntime
internal struct DataStoreData : DataStoreProtocol {
    public var symbolicName: String?
    public var uuid: String?
    public var capacity: String?
    public var freeSpace: String?
    public var type: String?

        enum CodingKeys: String, CodingKey {case symbolicName = "symbolicName"
        case uuid = "uuid"
        case capacity = "capacity"
        case freeSpace = "freeSpace"
        case type = "type"
        }

  public init()  {
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)
      if container.contains(.symbolicName) {
        self.symbolicName = try container.decode(String?.self, forKey: .symbolicName)
    }
    if container.contains(.uuid) {
        self.uuid = try container.decode(String?.self, forKey: .uuid)
    }
    if container.contains(.capacity) {
        self.capacity = try container.decode(String?.self, forKey: .capacity)
    }
    if container.contains(.freeSpace) {
        self.freeSpace = try container.decode(String?.self, forKey: .freeSpace)
    }
    if container.contains(.type) {
        self.type = try container.decode(String?.self, forKey: .type)
    }
    if var pageDecoder = decoder as? PageDecoder  {
      if pageDecoder.isPagedData,
        let nextLinkName = pageDecoder.nextLinkName {
          pageDecoder.nextLink = try UnknownCodingKey.decodeStringForKey(decoder: decoder, keyForDecode: nextLinkName)
      }
    }
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)
    if self.symbolicName != nil {try container.encode(self.symbolicName, forKey: .symbolicName)}
    if self.uuid != nil {try container.encode(self.uuid, forKey: .uuid)}
    if self.capacity != nil {try container.encode(self.capacity, forKey: .capacity)}
    if self.freeSpace != nil {try container.encode(self.freeSpace, forKey: .freeSpace)}
    if self.type != nil {try container.encode(self.type, forKey: .type)}
  }
}

extension DataFactory {
  public static func createDataStoreProtocol() -> DataStoreProtocol {
    return DataStoreData()
  }
}
