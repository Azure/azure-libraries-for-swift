// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License. See License.txt in the project root for license information.
//
// Code generated by Microsoft (R) AutoRest Code Generator.
import Foundation
import azureSwiftRuntime
internal struct SiteSealRequestData : SiteSealRequestProtocol {
    public var lightTheme: Bool?
    public var locale: String?

        enum CodingKeys: String, CodingKey {case lightTheme = "lightTheme"
        case locale = "locale"
        }

  public init()  {
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)
      if container.contains(.lightTheme) {
        self.lightTheme = try container.decode(Bool?.self, forKey: .lightTheme)
    }
    if container.contains(.locale) {
        self.locale = try container.decode(String?.self, forKey: .locale)
    }
    if var pageDecoder = decoder as? PageDecoder  {
      if pageDecoder.isPagedData,
        let nextLinkName = pageDecoder.nextLinkName {
          pageDecoder.nextLink = try UnknownCodingKey.decodeStringForKey(decoder: decoder, keyForDecode: nextLinkName)
      }
    }
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)
    if self.lightTheme != nil {try container.encode(self.lightTheme, forKey: .lightTheme)}
    if self.locale != nil {try container.encode(self.locale, forKey: .locale)}
  }
}

extension DataFactory {
  public static func createSiteSealRequestProtocol() -> SiteSealRequestProtocol {
    return SiteSealRequestData()
  }
}
