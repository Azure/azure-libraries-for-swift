// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License. See License.txt in the project root for license information.
//
// Code generated by Microsoft (R) AutoRest Code Generator.
import Foundation
import azureSwiftRuntime
internal struct ResourceMetricDefinitionPropertiesData : ResourceMetricDefinitionPropertiesProtocol {
    public var name: ResourceMetricNameProtocol?
    public var unit: String?
    public var primaryAggregationType: String?
    public var metricAvailabilities: [ResourceMetricAvailabilityProtocol?]?
    public var resourceUri: String?
    public var id: String?
    public var properties: [String:String]?

        enum CodingKeys: String, CodingKey {case name = "name"
        case unit = "unit"
        case primaryAggregationType = "primaryAggregationType"
        case metricAvailabilities = "metricAvailabilities"
        case resourceUri = "resourceUri"
        case id = "id"
        case properties = "properties"
        }

  public init()  {
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)
      if container.contains(.name) {
        self.name = try container.decode(ResourceMetricNameData?.self, forKey: .name)
    }
    if container.contains(.unit) {
        self.unit = try container.decode(String?.self, forKey: .unit)
    }
    if container.contains(.primaryAggregationType) {
        self.primaryAggregationType = try container.decode(String?.self, forKey: .primaryAggregationType)
    }
    if container.contains(.metricAvailabilities) {
        self.metricAvailabilities = try container.decode([ResourceMetricAvailabilityData?]?.self, forKey: .metricAvailabilities)
    }
    if container.contains(.resourceUri) {
        self.resourceUri = try container.decode(String?.self, forKey: .resourceUri)
    }
    if container.contains(.id) {
        self.id = try container.decode(String?.self, forKey: .id)
    }
    if container.contains(.properties) {
        self.properties = try container.decode([String:String]?.self, forKey: .properties)
    }
    if var pageDecoder = decoder as? PageDecoder  {
      if pageDecoder.isPagedData,
        let nextLinkName = pageDecoder.nextLinkName {
          pageDecoder.nextLink = try UnknownCodingKey.decodeStringForKey(decoder: decoder, keyForDecode: nextLinkName)
      }
    }
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)
    if self.name != nil {try container.encode(self.name as! ResourceMetricNameData?, forKey: .name)}
    if self.unit != nil {try container.encode(self.unit, forKey: .unit)}
    if self.primaryAggregationType != nil {try container.encode(self.primaryAggregationType, forKey: .primaryAggregationType)}
    if self.metricAvailabilities != nil {try container.encode(self.metricAvailabilities as! [ResourceMetricAvailabilityData?]?, forKey: .metricAvailabilities)}
    if self.resourceUri != nil {try container.encode(self.resourceUri, forKey: .resourceUri)}
    if self.id != nil {try container.encode(self.id, forKey: .id)}
    if self.properties != nil {try container.encode(self.properties, forKey: .properties)}
  }
}

extension DataFactory {
  public static func createResourceMetricDefinitionPropertiesProtocol() -> ResourceMetricDefinitionPropertiesProtocol {
    return ResourceMetricDefinitionPropertiesData()
  }
}
