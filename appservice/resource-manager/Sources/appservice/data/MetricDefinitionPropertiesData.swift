// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License. See License.txt in the project root for license information.
//
// Code generated by Microsoft (R) AutoRest Code Generator.
import Foundation
import azureSwiftRuntime
internal struct MetricDefinitionPropertiesData : MetricDefinitionPropertiesProtocol {
    public var name: String?
    public var unit: String?
    public var primaryAggregationType: String?
    public var metricAvailabilities: [MetricAvailabililyProtocol?]?
    public var displayName: String?

        enum CodingKeys: String, CodingKey {case name = "name"
        case unit = "unit"
        case primaryAggregationType = "primaryAggregationType"
        case metricAvailabilities = "metricAvailabilities"
        case displayName = "displayName"
        }

  public init()  {
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)
      if container.contains(.name) {
        self.name = try container.decode(String?.self, forKey: .name)
    }
    if container.contains(.unit) {
        self.unit = try container.decode(String?.self, forKey: .unit)
    }
    if container.contains(.primaryAggregationType) {
        self.primaryAggregationType = try container.decode(String?.self, forKey: .primaryAggregationType)
    }
    if container.contains(.metricAvailabilities) {
        self.metricAvailabilities = try container.decode([MetricAvailabililyData?]?.self, forKey: .metricAvailabilities)
    }
    if container.contains(.displayName) {
        self.displayName = try container.decode(String?.self, forKey: .displayName)
    }
    if var pageDecoder = decoder as? PageDecoder  {
      if pageDecoder.isPagedData,
        let nextLinkName = pageDecoder.nextLinkName {
          pageDecoder.nextLink = try UnknownCodingKey.decodeStringForKey(decoder: decoder, keyForDecode: nextLinkName)
      }
    }
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)
    if self.name != nil {try container.encode(self.name, forKey: .name)}
    if self.unit != nil {try container.encode(self.unit, forKey: .unit)}
    if self.primaryAggregationType != nil {try container.encode(self.primaryAggregationType, forKey: .primaryAggregationType)}
    if self.metricAvailabilities != nil {try container.encode(self.metricAvailabilities as! [MetricAvailabililyData?]?, forKey: .metricAvailabilities)}
    if self.displayName != nil {try container.encode(self.displayName, forKey: .displayName)}
  }
}

extension DataFactory {
  public static func createMetricDefinitionPropertiesProtocol() -> MetricDefinitionPropertiesProtocol {
    return MetricDefinitionPropertiesData()
  }
}
