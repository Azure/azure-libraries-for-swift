// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License. See License.txt in the project root for license information.
//
// Code generated by Microsoft (R) AutoRest Code Generator.
import Foundation
import azureSwiftRuntime
internal struct MSDeployLogPropertiesData : MSDeployLogPropertiesProtocol {
    public var entries: [MSDeployLogEntryProtocol?]?

        enum CodingKeys: String, CodingKey {case entries = "entries"
        }

  public init()  {
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)
      if container.contains(.entries) {
        self.entries = try container.decode([MSDeployLogEntryData?]?.self, forKey: .entries)
    }
    if var pageDecoder = decoder as? PageDecoder  {
      if pageDecoder.isPagedData,
        let nextLinkName = pageDecoder.nextLinkName {
          pageDecoder.nextLink = try UnknownCodingKey.decodeStringForKey(decoder: decoder, keyForDecode: nextLinkName)
      }
    }
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)
    if self.entries != nil {try container.encode(self.entries as! [MSDeployLogEntryData?]?, forKey: .entries)}
  }
}

extension DataFactory {
  public static func createMSDeployLogPropertiesProtocol() -> MSDeployLogPropertiesProtocol {
    return MSDeployLogPropertiesData()
  }
}
