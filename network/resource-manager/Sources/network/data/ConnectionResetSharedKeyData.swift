// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License. See License.txt in the project root for license information.
//
// Code generated by Microsoft (R) AutoRest Code Generator.
import Foundation
import azureSwiftRuntime
internal struct ConnectionResetSharedKeyData : ConnectionResetSharedKeyProtocol {
    public var keyLength: Int32

        enum CodingKeys: String, CodingKey {case keyLength = "keyLength"
        }

  public init(keyLength: Int32)  {
    self.keyLength = keyLength
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)
      self.keyLength = try container.decode(Int32.self, forKey: .keyLength)
    if var pageDecoder = decoder as? PageDecoder  {
      if pageDecoder.isPagedData,
        let nextLinkName = pageDecoder.nextLinkName {
          pageDecoder.nextLink = try UnknownCodingKey.decodeStringForKey(decoder: decoder, keyForDecode: nextLinkName)
      }
    }
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)
    try container.encode(self.keyLength, forKey: .keyLength)
  }
}

extension DataFactory {
  public static func createConnectionResetSharedKeyProtocol(keyLength: Int32) -> ConnectionResetSharedKeyProtocol {
    return ConnectionResetSharedKeyData(keyLength: keyLength)
  }
}
