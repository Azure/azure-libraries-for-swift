// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License. See License.txt in the project root for license information.
//
// Code generated by Microsoft (R) AutoRest Code Generator.
import Foundation
import azureSwiftRuntime
internal struct IPAddressAvailabilityResultData : IPAddressAvailabilityResultProtocol {
    public var available: Bool?
    public var availableIPAddresses: [String]?

        enum CodingKeys: String, CodingKey {case available = "available"
        case availableIPAddresses = "availableIPAddresses"
        }

  public init()  {
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)
      if container.contains(.available) {
        self.available = try container.decode(Bool?.self, forKey: .available)
    }
    if container.contains(.availableIPAddresses) {
        self.availableIPAddresses = try container.decode([String]?.self, forKey: .availableIPAddresses)
    }
    if var pageDecoder = decoder as? PageDecoder  {
      if pageDecoder.isPagedData,
        let nextLinkName = pageDecoder.nextLinkName {
          pageDecoder.nextLink = try UnknownCodingKey.decodeStringForKey(decoder: decoder, keyForDecode: nextLinkName)
      }
    }
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)
    if self.available != nil {try container.encode(self.available, forKey: .available)}
    if self.availableIPAddresses != nil {try container.encode(self.availableIPAddresses as! [String]?, forKey: .availableIPAddresses)}
  }
}

extension DataFactory {
  public static func createIPAddressAvailabilityResultProtocol() -> IPAddressAvailabilityResultProtocol {
    return IPAddressAvailabilityResultData()
  }
}
