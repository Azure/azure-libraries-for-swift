// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License. See License.txt in the project root for license information.
//
// Code generated by Microsoft (R) AutoRest Code Generator.
import Foundation
import azureSwiftRuntime
internal struct RegistryListCredentialsResultData : RegistryListCredentialsResultProtocol {
    public var username: String?
    public var passwords: [RegistryPasswordProtocol?]?

        enum CodingKeys: String, CodingKey {case username = "username"
        case passwords = "passwords"
        }

  public init()  {
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)
      if container.contains(.username) {
        self.username = try container.decode(String?.self, forKey: .username)
    }
    if container.contains(.passwords) {
        self.passwords = try container.decode([RegistryPasswordData?]?.self, forKey: .passwords)
    }
    if var pageDecoder = decoder as? PageDecoder  {
      if pageDecoder.isPagedData,
        let nextLinkName = pageDecoder.nextLinkName {
          pageDecoder.nextLink = try UnknownCodingKey.decodeStringForKey(decoder: decoder, keyForDecode: nextLinkName)
      }
    }
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)
    if self.username != nil {try container.encode(self.username, forKey: .username)}
    if self.passwords != nil {try container.encode(self.passwords as! [RegistryPasswordData?]?, forKey: .passwords)}
  }
}

extension DataFactory {
  public static func createRegistryListCredentialsResultProtocol() -> RegistryListCredentialsResultProtocol {
    return RegistryListCredentialsResultData()
  }
}
