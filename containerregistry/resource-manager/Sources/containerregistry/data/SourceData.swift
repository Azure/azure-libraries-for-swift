// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License. See License.txt in the project root for license information.
//
// Code generated by Microsoft (R) AutoRest Code Generator.
import Foundation
import azureSwiftRuntime
internal struct SourceData : SourceProtocol {
    public var addr: String?
    public var instanceID: String?

        enum CodingKeys: String, CodingKey {case addr = "addr"
        case instanceID = "instanceID"
        }

  public init()  {
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)
      if container.contains(.addr) {
        self.addr = try container.decode(String?.self, forKey: .addr)
    }
    if container.contains(.instanceID) {
        self.instanceID = try container.decode(String?.self, forKey: .instanceID)
    }
    if var pageDecoder = decoder as? PageDecoder  {
      if pageDecoder.isPagedData,
        let nextLinkName = pageDecoder.nextLinkName {
          pageDecoder.nextLink = try UnknownCodingKey.decodeStringForKey(decoder: decoder, keyForDecode: nextLinkName)
      }
    }
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)
    if self.addr != nil {try container.encode(self.addr, forKey: .addr)}
    if self.instanceID != nil {try container.encode(self.instanceID, forKey: .instanceID)}
  }
}

extension DataFactory {
  public static func createSourceProtocol() -> SourceProtocol {
    return SourceData()
  }
}
