// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License. See License.txt in the project root for license information.
//
// Code generated by Microsoft (R) AutoRest Code Generator.
import Foundation
import azureSwiftRuntime
internal struct EventResponseMessageData : EventResponseMessageProtocol {
    public var content: String?
    public var headers: [String:String]?
    public var reasonPhrase: String?
    public var statusCode: String?
    public var version: String?

        enum CodingKeys: String, CodingKey {case content = "content"
        case headers = "headers"
        case reasonPhrase = "reasonPhrase"
        case statusCode = "statusCode"
        case version = "version"
        }

  public init()  {
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)
      if container.contains(.content) {
        self.content = try container.decode(String?.self, forKey: .content)
    }
    if container.contains(.headers) {
        self.headers = try container.decode([String:String]?.self, forKey: .headers)
    }
    if container.contains(.reasonPhrase) {
        self.reasonPhrase = try container.decode(String?.self, forKey: .reasonPhrase)
    }
    if container.contains(.statusCode) {
        self.statusCode = try container.decode(String?.self, forKey: .statusCode)
    }
    if container.contains(.version) {
        self.version = try container.decode(String?.self, forKey: .version)
    }
    if var pageDecoder = decoder as? PageDecoder  {
      if pageDecoder.isPagedData,
        let nextLinkName = pageDecoder.nextLinkName {
          pageDecoder.nextLink = try UnknownCodingKey.decodeStringForKey(decoder: decoder, keyForDecode: nextLinkName)
      }
    }
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)
    if self.content != nil {try container.encode(self.content, forKey: .content)}
    if self.headers != nil {try container.encode(self.headers, forKey: .headers)}
    if self.reasonPhrase != nil {try container.encode(self.reasonPhrase, forKey: .reasonPhrase)}
    if self.statusCode != nil {try container.encode(self.statusCode, forKey: .statusCode)}
    if self.version != nil {try container.encode(self.version, forKey: .version)}
  }
}

extension DataFactory {
  public static func createEventResponseMessageProtocol() -> EventResponseMessageProtocol {
    return EventResponseMessageData()
  }
}
