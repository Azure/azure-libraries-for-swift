// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License. See License.txt in the project root for license information.
//
// Code generated by Microsoft (R) AutoRest Code Generator.
import Foundation
import azureSwiftRuntime
internal struct EventRequestMessageData : EventRequestMessageProtocol {
    public var content: EventContentProtocol?
    public var headers: [String:String]?
    public var method: String?
    public var requestUri: String?
    public var version: String?

        enum CodingKeys: String, CodingKey {case content = "content"
        case headers = "headers"
        case method = "method"
        case requestUri = "requestUri"
        case version = "version"
        }

  public init()  {
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)
      if container.contains(.content) {
        self.content = try container.decode(EventContentData?.self, forKey: .content)
    }
    if container.contains(.headers) {
        self.headers = try container.decode([String:String]?.self, forKey: .headers)
    }
    if container.contains(.method) {
        self.method = try container.decode(String?.self, forKey: .method)
    }
    if container.contains(.requestUri) {
        self.requestUri = try container.decode(String?.self, forKey: .requestUri)
    }
    if container.contains(.version) {
        self.version = try container.decode(String?.self, forKey: .version)
    }
    if var pageDecoder = decoder as? PageDecoder  {
      if pageDecoder.isPagedData,
        let nextLinkName = pageDecoder.nextLinkName {
          pageDecoder.nextLink = try UnknownCodingKey.decodeStringForKey(decoder: decoder, keyForDecode: nextLinkName)
      }
    }
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)
    if self.content != nil {try container.encode(self.content as! EventContentData?, forKey: .content)}
    if self.headers != nil {try container.encode(self.headers, forKey: .headers)}
    if self.method != nil {try container.encode(self.method, forKey: .method)}
    if self.requestUri != nil {try container.encode(self.requestUri, forKey: .requestUri)}
    if self.version != nil {try container.encode(self.version, forKey: .version)}
  }
}

extension DataFactory {
  public static func createEventRequestMessageProtocol() -> EventRequestMessageProtocol {
    return EventRequestMessageData()
  }
}
