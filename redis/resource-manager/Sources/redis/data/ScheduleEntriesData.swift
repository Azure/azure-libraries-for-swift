// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License. See License.txt in the project root for license information.
//
// Code generated by Microsoft (R) AutoRest Code Generator.
import Foundation
import azureSwiftRuntime
internal struct ScheduleEntriesData : ScheduleEntriesProtocol {
    public var scheduleEntries: [ScheduleEntryProtocol]

        enum CodingKeys: String, CodingKey {case scheduleEntries = "scheduleEntries"
        }

  public init(scheduleEntries: [ScheduleEntryProtocol])  {
    self.scheduleEntries = scheduleEntries
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)
      self.scheduleEntries = try container.decode([ScheduleEntryData].self, forKey: .scheduleEntries)
    if var pageDecoder = decoder as? PageDecoder  {
      if pageDecoder.isPagedData,
        let nextLinkName = pageDecoder.nextLinkName {
          pageDecoder.nextLink = try UnknownCodingKey.decodeStringForKey(decoder: decoder, keyForDecode: nextLinkName)
      }
    }
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)
    try container.encode(self.scheduleEntries as! [ScheduleEntryData], forKey: .scheduleEntries)
  }
}

extension DataFactory {
  public static func createScheduleEntriesProtocol(scheduleEntries: [ScheduleEntryProtocol]) -> ScheduleEntriesProtocol {
    return ScheduleEntriesData(scheduleEntries: scheduleEntries)
  }
}
