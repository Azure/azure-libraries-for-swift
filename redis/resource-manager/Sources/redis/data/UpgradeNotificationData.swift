// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License. See License.txt in the project root for license information.
//
// Code generated by Microsoft (R) AutoRest Code Generator.
import Foundation
import azureSwiftRuntime
internal struct UpgradeNotificationData : UpgradeNotificationProtocol {
    public var name: String?
    public var timestamp: Date?
    public var upsellNotification: [String:String]?

        enum CodingKeys: String, CodingKey {case name = "name"
        case timestamp = "timestamp"
        case upsellNotification = "upsellNotification"
        }

  public init()  {
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)
      if container.contains(.name) {
        self.name = try container.decode(String?.self, forKey: .name)
    }
    if container.contains(.timestamp) {
        self.timestamp = DateConverter.fromString(dateStr: (try container.decode(String?.self, forKey: .timestamp)), format: .dateTime)
    }
    if container.contains(.upsellNotification) {
        self.upsellNotification = try container.decode([String:String]?.self, forKey: .upsellNotification)
    }
    if var pageDecoder = decoder as? PageDecoder  {
      if pageDecoder.isPagedData,
        let nextLinkName = pageDecoder.nextLinkName {
          pageDecoder.nextLink = try UnknownCodingKey.decodeStringForKey(decoder: decoder, keyForDecode: nextLinkName)
      }
    }
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)
    if self.name != nil {try container.encode(self.name, forKey: .name)}
    if self.timestamp != nil {
        try container.encode(DateConverter.toString(date: self.timestamp!, format: .dateTime), forKey: .timestamp)
    }
    if self.upsellNotification != nil {try container.encode(self.upsellNotification, forKey: .upsellNotification)}
  }
}

extension DataFactory {
  public static func createUpgradeNotificationProtocol() -> UpgradeNotificationProtocol {
    return UpgradeNotificationData()
  }
}
