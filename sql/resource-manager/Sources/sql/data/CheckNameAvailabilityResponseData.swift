// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License. See License.txt in the project root for license information.
//
// Code generated by Microsoft (R) AutoRest Code Generator.
import Foundation
import azureSwiftRuntime
internal struct CheckNameAvailabilityResponseData : CheckNameAvailabilityResponseProtocol {
    public var available: Bool?
    public var message: String?
    public var name: String?
    public var reason: CheckNameAvailabilityReasonEnum?

        enum CodingKeys: String, CodingKey {case available = "available"
        case message = "message"
        case name = "name"
        case reason = "reason"
        }

  public init()  {
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)
      if container.contains(.available) {
        self.available = try container.decode(Bool?.self, forKey: .available)
    }
    if container.contains(.message) {
        self.message = try container.decode(String?.self, forKey: .message)
    }
    if container.contains(.name) {
        self.name = try container.decode(String?.self, forKey: .name)
    }
    if container.contains(.reason) {
        self.reason = try container.decode(CheckNameAvailabilityReasonEnum?.self, forKey: .reason)
    }
    if var pageDecoder = decoder as? PageDecoder  {
      if pageDecoder.isPagedData,
        let nextLinkName = pageDecoder.nextLinkName {
          pageDecoder.nextLink = try UnknownCodingKey.decodeStringForKey(decoder: decoder, keyForDecode: nextLinkName)
      }
    }
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)
    if self.available != nil {try container.encode(self.available, forKey: .available)}
    if self.message != nil {try container.encode(self.message, forKey: .message)}
    if self.name != nil {try container.encode(self.name, forKey: .name)}
    if self.reason != nil {try container.encode(self.reason, forKey: .reason)}
  }
}

extension DataFactory {
  public static func createCheckNameAvailabilityResponseProtocol() -> CheckNameAvailabilityResponseProtocol {
    return CheckNameAvailabilityResponseData()
  }
}
