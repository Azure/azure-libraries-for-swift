// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License. See License.txt in the project root for license information.
//
// Code generated by Microsoft (R) AutoRest Code Generator.
import Foundation
import azureSwiftRuntime
internal struct SyncMemberPropertiesData : SyncMemberPropertiesProtocol {
    public var databaseType: SyncMemberDbTypeEnum?
    public var syncAgentId: String?
    public var sqlServerDatabaseId: String?
    public var serverName: String?
    public var databaseName: String?
    public var userName: String?
    public var password: String?
    public var syncDirection: SyncDirectionEnum?
    public var syncState: SyncMemberStateEnum?

        enum CodingKeys: String, CodingKey {case databaseType = "databaseType"
        case syncAgentId = "syncAgentId"
        case sqlServerDatabaseId = "sqlServerDatabaseId"
        case serverName = "serverName"
        case databaseName = "databaseName"
        case userName = "userName"
        case password = "password"
        case syncDirection = "syncDirection"
        case syncState = "syncState"
        }

  public init()  {
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)
      if container.contains(.databaseType) {
        self.databaseType = try container.decode(SyncMemberDbTypeEnum?.self, forKey: .databaseType)
    }
    if container.contains(.syncAgentId) {
        self.syncAgentId = try container.decode(String?.self, forKey: .syncAgentId)
    }
    if container.contains(.sqlServerDatabaseId) {
        self.sqlServerDatabaseId = try container.decode(String?.self, forKey: .sqlServerDatabaseId)
    }
    if container.contains(.serverName) {
        self.serverName = try container.decode(String?.self, forKey: .serverName)
    }
    if container.contains(.databaseName) {
        self.databaseName = try container.decode(String?.self, forKey: .databaseName)
    }
    if container.contains(.userName) {
        self.userName = try container.decode(String?.self, forKey: .userName)
    }
    if container.contains(.password) {
        self.password = try container.decode(String?.self, forKey: .password)
    }
    if container.contains(.syncDirection) {
        self.syncDirection = try container.decode(SyncDirectionEnum?.self, forKey: .syncDirection)
    }
    if container.contains(.syncState) {
        self.syncState = try container.decode(SyncMemberStateEnum?.self, forKey: .syncState)
    }
    if var pageDecoder = decoder as? PageDecoder  {
      if pageDecoder.isPagedData,
        let nextLinkName = pageDecoder.nextLinkName {
          pageDecoder.nextLink = try UnknownCodingKey.decodeStringForKey(decoder: decoder, keyForDecode: nextLinkName)
      }
    }
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)
    if self.databaseType != nil {try container.encode(self.databaseType, forKey: .databaseType)}
    if self.syncAgentId != nil {try container.encode(self.syncAgentId, forKey: .syncAgentId)}
    if self.sqlServerDatabaseId != nil {try container.encode(self.sqlServerDatabaseId, forKey: .sqlServerDatabaseId)}
    if self.serverName != nil {try container.encode(self.serverName, forKey: .serverName)}
    if self.databaseName != nil {try container.encode(self.databaseName, forKey: .databaseName)}
    if self.userName != nil {try container.encode(self.userName, forKey: .userName)}
    if self.password != nil {try container.encode(self.password, forKey: .password)}
    if self.syncDirection != nil {try container.encode(self.syncDirection, forKey: .syncDirection)}
    if self.syncState != nil {try container.encode(self.syncState, forKey: .syncState)}
  }
}

extension DataFactory {
  public static func createSyncMemberPropertiesProtocol() -> SyncMemberPropertiesProtocol {
    return SyncMemberPropertiesData()
  }
}
