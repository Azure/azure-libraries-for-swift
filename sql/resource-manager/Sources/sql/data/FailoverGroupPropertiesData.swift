// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License. See License.txt in the project root for license information.
//
// Code generated by Microsoft (R) AutoRest Code Generator.
import Foundation
import azureSwiftRuntime
internal struct FailoverGroupPropertiesData : FailoverGroupPropertiesProtocol {
    public var readWriteEndpoint: FailoverGroupReadWriteEndpointProtocol
    public var readOnlyEndpoint: FailoverGroupReadOnlyEndpointProtocol?
    public var replicationRole: FailoverGroupReplicationRoleEnum?
    public var replicationState: String?
    public var partnerServers: [PartnerInfoProtocol]
    public var databases: [String]?

        enum CodingKeys: String, CodingKey {case readWriteEndpoint = "readWriteEndpoint"
        case readOnlyEndpoint = "readOnlyEndpoint"
        case replicationRole = "replicationRole"
        case replicationState = "replicationState"
        case partnerServers = "partnerServers"
        case databases = "databases"
        }

  public init(readWriteEndpoint: FailoverGroupReadWriteEndpointProtocol, partnerServers: [PartnerInfoProtocol])  {
    self.readWriteEndpoint = readWriteEndpoint
    self.partnerServers = partnerServers
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)
      self.readWriteEndpoint = try container.decode(FailoverGroupReadWriteEndpointData.self, forKey: .readWriteEndpoint)
    if container.contains(.readOnlyEndpoint) {
        self.readOnlyEndpoint = try container.decode(FailoverGroupReadOnlyEndpointData?.self, forKey: .readOnlyEndpoint)
    }
    if container.contains(.replicationRole) {
        self.replicationRole = try container.decode(FailoverGroupReplicationRoleEnum?.self, forKey: .replicationRole)
    }
    if container.contains(.replicationState) {
        self.replicationState = try container.decode(String?.self, forKey: .replicationState)
    }
    self.partnerServers = try container.decode([PartnerInfoData].self, forKey: .partnerServers)
    if container.contains(.databases) {
        self.databases = try container.decode([String]?.self, forKey: .databases)
    }
    if var pageDecoder = decoder as? PageDecoder  {
      if pageDecoder.isPagedData,
        let nextLinkName = pageDecoder.nextLinkName {
          pageDecoder.nextLink = try UnknownCodingKey.decodeStringForKey(decoder: decoder, keyForDecode: nextLinkName)
      }
    }
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)
    try container.encode(self.readWriteEndpoint as! FailoverGroupReadWriteEndpointData, forKey: .readWriteEndpoint)
    if self.readOnlyEndpoint != nil {try container.encode(self.readOnlyEndpoint as! FailoverGroupReadOnlyEndpointData?, forKey: .readOnlyEndpoint)}
    if self.replicationRole != nil {try container.encode(self.replicationRole, forKey: .replicationRole)}
    if self.replicationState != nil {try container.encode(self.replicationState, forKey: .replicationState)}
    try container.encode(self.partnerServers as! [PartnerInfoData], forKey: .partnerServers)
    if self.databases != nil {try container.encode(self.databases as! [String]?, forKey: .databases)}
  }
}

extension DataFactory {
  public static func createFailoverGroupPropertiesProtocol(readWriteEndpoint: FailoverGroupReadWriteEndpointProtocol, partnerServers: [PartnerInfoProtocol]) -> FailoverGroupPropertiesProtocol {
    return FailoverGroupPropertiesData(readWriteEndpoint: readWriteEndpoint, partnerServers: partnerServers)
  }
}
