// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License. See License.txt in the project root for license information.
//
// Code generated by Microsoft (R) AutoRest Code Generator.
import Foundation
import azureSwiftRuntime
internal struct StatisticsData : StatisticsProtocol {
    public var counterProperty: String?
    public var counterValue: Int64?
    public var startTime: Date?
    public var endTime: Date?
    public var id: String?

        enum CodingKeys: String, CodingKey {case counterProperty = "counterProperty"
        case counterValue = "counterValue"
        case startTime = "startTime"
        case endTime = "endTime"
        case id = "id"
        }

  public init()  {
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)
      if container.contains(.counterProperty) {
        self.counterProperty = try container.decode(String?.self, forKey: .counterProperty)
    }
    if container.contains(.counterValue) {
        self.counterValue = try container.decode(Int64?.self, forKey: .counterValue)
    }
    if container.contains(.startTime) {
        self.startTime = DateConverter.fromString(dateStr: (try container.decode(String?.self, forKey: .startTime)), format: .dateTime)
    }
    if container.contains(.endTime) {
        self.endTime = DateConverter.fromString(dateStr: (try container.decode(String?.self, forKey: .endTime)), format: .dateTime)
    }
    if container.contains(.id) {
        self.id = try container.decode(String?.self, forKey: .id)
    }
    if var pageDecoder = decoder as? PageDecoder  {
      if pageDecoder.isPagedData,
        let nextLinkName = pageDecoder.nextLinkName {
          pageDecoder.nextLink = try UnknownCodingKey.decodeStringForKey(decoder: decoder, keyForDecode: nextLinkName)
      }
    }
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)
    if self.counterProperty != nil {try container.encode(self.counterProperty, forKey: .counterProperty)}
    if self.counterValue != nil {try container.encode(self.counterValue, forKey: .counterValue)}
    if self.startTime != nil {
        try container.encode(DateConverter.toString(date: self.startTime!, format: .dateTime), forKey: .startTime)
    }
    if self.endTime != nil {
        try container.encode(DateConverter.toString(date: self.endTime!, format: .dateTime), forKey: .endTime)
    }
    if self.id != nil {try container.encode(self.id, forKey: .id)}
  }
}

extension DataFactory {
  public static func createStatisticsProtocol() -> StatisticsProtocol {
    return StatisticsData()
  }
}
