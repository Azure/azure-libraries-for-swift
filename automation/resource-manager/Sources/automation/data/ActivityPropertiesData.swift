// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License. See License.txt in the project root for license information.
//
// Code generated by Microsoft (R) AutoRest Code Generator.
import Foundation
import azureSwiftRuntime
internal struct ActivityPropertiesData : ActivityPropertiesProtocol {
    public var definition: String?
    public var parameterSets: [ActivityParameterSetProtocol?]?
    public var outputTypes: [ActivityOutputTypeProtocol?]?
    public var creationTime: Date?
    public var lastModifiedTime: Date?
    public var description: String?

        enum CodingKeys: String, CodingKey {case definition = "definition"
        case parameterSets = "parameterSets"
        case outputTypes = "outputTypes"
        case creationTime = "creationTime"
        case lastModifiedTime = "lastModifiedTime"
        case description = "description"
        }

  public init()  {
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)
      if container.contains(.definition) {
        self.definition = try container.decode(String?.self, forKey: .definition)
    }
    if container.contains(.parameterSets) {
        self.parameterSets = try container.decode([ActivityParameterSetData?]?.self, forKey: .parameterSets)
    }
    if container.contains(.outputTypes) {
        self.outputTypes = try container.decode([ActivityOutputTypeData?]?.self, forKey: .outputTypes)
    }
    if container.contains(.creationTime) {
        self.creationTime = DateConverter.fromString(dateStr: (try container.decode(String?.self, forKey: .creationTime)), format: .dateTime)
    }
    if container.contains(.lastModifiedTime) {
        self.lastModifiedTime = DateConverter.fromString(dateStr: (try container.decode(String?.self, forKey: .lastModifiedTime)), format: .dateTime)
    }
    if container.contains(.description) {
        self.description = try container.decode(String?.self, forKey: .description)
    }
    if var pageDecoder = decoder as? PageDecoder  {
      if pageDecoder.isPagedData,
        let nextLinkName = pageDecoder.nextLinkName {
          pageDecoder.nextLink = try UnknownCodingKey.decodeStringForKey(decoder: decoder, keyForDecode: nextLinkName)
      }
    }
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)
    if self.definition != nil {try container.encode(self.definition, forKey: .definition)}
    if self.parameterSets != nil {try container.encode(self.parameterSets as! [ActivityParameterSetData?]?, forKey: .parameterSets)}
    if self.outputTypes != nil {try container.encode(self.outputTypes as! [ActivityOutputTypeData?]?, forKey: .outputTypes)}
    if self.creationTime != nil {
        try container.encode(DateConverter.toString(date: self.creationTime!, format: .dateTime), forKey: .creationTime)
    }
    if self.lastModifiedTime != nil {
        try container.encode(DateConverter.toString(date: self.lastModifiedTime!, format: .dateTime), forKey: .lastModifiedTime)
    }
    if self.description != nil {try container.encode(self.description, forKey: .description)}
  }
}

extension DataFactory {
  public static func createActivityPropertiesProtocol() -> ActivityPropertiesProtocol {
    return ActivityPropertiesData()
  }
}
