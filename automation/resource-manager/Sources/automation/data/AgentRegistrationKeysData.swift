// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License. See License.txt in the project root for license information.
//
// Code generated by Microsoft (R) AutoRest Code Generator.
import Foundation
import azureSwiftRuntime
internal struct AgentRegistrationKeysData : AgentRegistrationKeysProtocol {
    public var primary: String?
    public var secondary: String?

        enum CodingKeys: String, CodingKey {case primary = "primary"
        case secondary = "secondary"
        }

  public init()  {
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)
      if container.contains(.primary) {
        self.primary = try container.decode(String?.self, forKey: .primary)
    }
    if container.contains(.secondary) {
        self.secondary = try container.decode(String?.self, forKey: .secondary)
    }
    if var pageDecoder = decoder as? PageDecoder  {
      if pageDecoder.isPagedData,
        let nextLinkName = pageDecoder.nextLinkName {
          pageDecoder.nextLink = try UnknownCodingKey.decodeStringForKey(decoder: decoder, keyForDecode: nextLinkName)
      }
    }
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)
    if self.primary != nil {try container.encode(self.primary, forKey: .primary)}
    if self.secondary != nil {try container.encode(self.secondary, forKey: .secondary)}
  }
}

extension DataFactory {
  public static func createAgentRegistrationKeysProtocol() -> AgentRegistrationKeysProtocol {
    return AgentRegistrationKeysData()
  }
}
