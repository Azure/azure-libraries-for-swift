// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License. See License.txt in the project root for license information.
//
// Code generated by Microsoft (R) AutoRest Code Generator.
import Foundation
import azureSwiftRuntime
internal struct HttpRequestInfoData : HttpRequestInfoProtocol {
    public var clientRequestId: String?
    public var clientIpAddress: String?
    public var method: String?
    public var uri: String?

        enum CodingKeys: String, CodingKey {case clientRequestId = "clientRequestId"
        case clientIpAddress = "clientIpAddress"
        case method = "method"
        case uri = "uri"
        }

  public init()  {
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)
      if container.contains(.clientRequestId) {
        self.clientRequestId = try container.decode(String?.self, forKey: .clientRequestId)
    }
    if container.contains(.clientIpAddress) {
        self.clientIpAddress = try container.decode(String?.self, forKey: .clientIpAddress)
    }
    if container.contains(.method) {
        self.method = try container.decode(String?.self, forKey: .method)
    }
    if container.contains(.uri) {
        self.uri = try container.decode(String?.self, forKey: .uri)
    }
    if var pageDecoder = decoder as? PageDecoder  {
      if pageDecoder.isPagedData,
        let nextLinkName = pageDecoder.nextLinkName {
          pageDecoder.nextLink = try UnknownCodingKey.decodeStringForKey(decoder: decoder, keyForDecode: nextLinkName)
      }
    }
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)
    if self.clientRequestId != nil {try container.encode(self.clientRequestId, forKey: .clientRequestId)}
    if self.clientIpAddress != nil {try container.encode(self.clientIpAddress, forKey: .clientIpAddress)}
    if self.method != nil {try container.encode(self.method, forKey: .method)}
    if self.uri != nil {try container.encode(self.uri, forKey: .uri)}
  }
}

extension DataFactory {
  public static func createHttpRequestInfoProtocol() -> HttpRequestInfoProtocol {
    return HttpRequestInfoData()
  }
}
