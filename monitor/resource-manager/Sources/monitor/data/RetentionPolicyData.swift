// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License. See License.txt in the project root for license information.
//
// Code generated by Microsoft (R) AutoRest Code Generator.
import Foundation
import azureSwiftRuntime
internal struct RetentionPolicyData : RetentionPolicyProtocol {
    public var enabled: Bool
    public var days: Int32

        enum CodingKeys: String, CodingKey {case enabled = "enabled"
        case days = "days"
        }

  public init(enabled: Bool, days: Int32)  {
    self.enabled = enabled
    self.days = days
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)
      self.enabled = try container.decode(Bool.self, forKey: .enabled)
    self.days = try container.decode(Int32.self, forKey: .days)
    if var pageDecoder = decoder as? PageDecoder  {
      if pageDecoder.isPagedData,
        let nextLinkName = pageDecoder.nextLinkName {
          pageDecoder.nextLink = try UnknownCodingKey.decodeStringForKey(decoder: decoder, keyForDecode: nextLinkName)
      }
    }
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)
    try container.encode(self.enabled, forKey: .enabled)
    try container.encode(self.days, forKey: .days)
  }
}

extension DataFactory {
  public static func createRetentionPolicyProtocol(enabled: Bool, days: Int32) -> RetentionPolicyProtocol {
    return RetentionPolicyData(enabled: enabled, days: days)
  }
}
