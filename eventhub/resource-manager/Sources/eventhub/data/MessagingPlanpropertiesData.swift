// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License. See License.txt in the project root for license information.
//
// Code generated by Microsoft (R) AutoRest Code Generator.
import Foundation
import azureSwiftRuntime
internal struct MessagingPlanpropertiesData : MessagingPlanpropertiesProtocol {
    public var sku: Int32?
    public var selectedEventHubUnit: Int32?
    public var updatedAt: Date?
    public var revision: Int64?

        enum CodingKeys: String, CodingKey {case sku = "sku"
        case selectedEventHubUnit = "selectedEventHubUnit"
        case updatedAt = "updatedAt"
        case revision = "revision"
        }

  public init()  {
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)
      if container.contains(.sku) {
        self.sku = try container.decode(Int32?.self, forKey: .sku)
    }
    if container.contains(.selectedEventHubUnit) {
        self.selectedEventHubUnit = try container.decode(Int32?.self, forKey: .selectedEventHubUnit)
    }
    if container.contains(.updatedAt) {
        self.updatedAt = DateConverter.fromString(dateStr: (try container.decode(String?.self, forKey: .updatedAt)), format: .dateTime)
    }
    if container.contains(.revision) {
        self.revision = try container.decode(Int64?.self, forKey: .revision)
    }
    if var pageDecoder = decoder as? PageDecoder  {
      if pageDecoder.isPagedData,
        let nextLinkName = pageDecoder.nextLinkName {
          pageDecoder.nextLink = try UnknownCodingKey.decodeStringForKey(decoder: decoder, keyForDecode: nextLinkName)
      }
    }
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)
    if self.sku != nil {try container.encode(self.sku, forKey: .sku)}
    if self.selectedEventHubUnit != nil {try container.encode(self.selectedEventHubUnit, forKey: .selectedEventHubUnit)}
    if self.updatedAt != nil {
        try container.encode(DateConverter.toString(date: self.updatedAt!, format: .dateTime), forKey: .updatedAt)
    }
    if self.revision != nil {try container.encode(self.revision, forKey: .revision)}
  }
}

extension DataFactory {
  public static func createMessagingPlanpropertiesProtocol() -> MessagingPlanpropertiesProtocol {
    return MessagingPlanpropertiesData()
  }
}
