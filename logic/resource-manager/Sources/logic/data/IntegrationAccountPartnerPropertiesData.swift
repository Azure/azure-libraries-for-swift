// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License. See License.txt in the project root for license information.
//
// Code generated by Microsoft (R) AutoRest Code Generator.
import Foundation
import azureSwiftRuntime
internal struct IntegrationAccountPartnerPropertiesData : IntegrationAccountPartnerPropertiesProtocol {
    public var partnerType: PartnerTypeEnum
    public var createdTime: Date?
    public var changedTime: Date?
    public var metadata: [String: String?]?
    public var content: PartnerContentProtocol

        enum CodingKeys: String, CodingKey {case partnerType = "partnerType"
        case createdTime = "createdTime"
        case changedTime = "changedTime"
        case metadata = "metadata"
        case content = "content"
        }

  public init(partnerType: PartnerTypeEnum, content: PartnerContentProtocol)  {
    self.partnerType = partnerType
    self.content = content
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)
      self.partnerType = try container.decode(PartnerTypeEnum.self, forKey: .partnerType)
    if container.contains(.createdTime) {
        self.createdTime = DateConverter.fromString(dateStr: (try container.decode(String?.self, forKey: .createdTime)), format: .dateTime)
    }
    if container.contains(.changedTime) {
        self.changedTime = DateConverter.fromString(dateStr: (try container.decode(String?.self, forKey: .changedTime)), format: .dateTime)
    }
    if container.contains(.metadata) {
        self.metadata = try container.decode([String: String?]?.self, forKey: .metadata)
    }
    self.content = try container.decode(PartnerContentData.self, forKey: .content)
    if var pageDecoder = decoder as? PageDecoder  {
      if pageDecoder.isPagedData,
        let nextLinkName = pageDecoder.nextLinkName {
          pageDecoder.nextLink = try UnknownCodingKey.decodeStringForKey(decoder: decoder, keyForDecode: nextLinkName)
      }
    }
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)
    try container.encode(self.partnerType, forKey: .partnerType)
    if self.createdTime != nil {
        try container.encode(DateConverter.toString(date: self.createdTime!, format: .dateTime), forKey: .createdTime)
    }
    if self.changedTime != nil {
        try container.encode(DateConverter.toString(date: self.changedTime!, format: .dateTime), forKey: .changedTime)
    }
    if self.metadata != nil {try container.encode(self.metadata, forKey: .metadata)}
    try container.encode(self.content as! PartnerContentData, forKey: .content)
  }
}

extension DataFactory {
  public static func createIntegrationAccountPartnerPropertiesProtocol(partnerType: PartnerTypeEnum, content: PartnerContentProtocol) -> IntegrationAccountPartnerPropertiesProtocol {
    return IntegrationAccountPartnerPropertiesData(partnerType: partnerType, content: content)
  }
}
