// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License. See License.txt in the project root for license information.
//
// Code generated by Microsoft (R) AutoRest Code Generator.
import Foundation
import azureSwiftRuntime
internal struct AzureSkuData : AzureSkuProtocol {
    public var name: String
    public var tier: String

        enum CodingKeys: String, CodingKey {case name = "name"
        case tier = "tier"
        }

  public init(name: String, tier: String)  {
    self.name = name
    self.tier = tier
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)
      self.name = try container.decode(String.self, forKey: .name)
    self.tier = try container.decode(String.self, forKey: .tier)
    if var pageDecoder = decoder as? PageDecoder  {
      if pageDecoder.isPagedData,
        let nextLinkName = pageDecoder.nextLinkName {
          pageDecoder.nextLink = try UnknownCodingKey.decodeStringForKey(decoder: decoder, keyForDecode: nextLinkName)
      }
    }
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)
    try container.encode(self.name, forKey: .name)
    try container.encode(self.tier, forKey: .tier)
  }
}

extension DataFactory {
  public static func createAzureSkuProtocol(name: String, tier: String) -> AzureSkuProtocol {
    return AzureSkuData(name: name, tier: tier)
  }
}
