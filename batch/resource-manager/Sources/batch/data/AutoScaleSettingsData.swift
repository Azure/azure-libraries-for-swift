// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License. See License.txt in the project root for license information.
//
// Code generated by Microsoft (R) AutoRest Code Generator.
import Foundation
import azureSwiftRuntime
internal struct AutoScaleSettingsData : AutoScaleSettingsProtocol {
    public var formula: String
    public var evaluationInterval: String?

        enum CodingKeys: String, CodingKey {case formula = "formula"
        case evaluationInterval = "evaluationInterval"
        }

  public init(formula: String)  {
    self.formula = formula
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)
      self.formula = try container.decode(String.self, forKey: .formula)
    if container.contains(.evaluationInterval) {
        self.evaluationInterval = try container.decode(String?.self, forKey: .evaluationInterval)
    }
    if var pageDecoder = decoder as? PageDecoder  {
      if pageDecoder.isPagedData,
        let nextLinkName = pageDecoder.nextLinkName {
          pageDecoder.nextLink = try UnknownCodingKey.decodeStringForKey(decoder: decoder, keyForDecode: nextLinkName)
      }
    }
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)
    try container.encode(self.formula, forKey: .formula)
    if self.evaluationInterval != nil {try container.encode(self.evaluationInterval, forKey: .evaluationInterval)}
  }
}

extension DataFactory {
  public static func createAutoScaleSettingsProtocol(formula: String) -> AutoScaleSettingsProtocol {
    return AutoScaleSettingsData(formula: formula)
  }
}
