// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License. See License.txt in the project root for license information.
//
// Code generated by Microsoft (R) AutoRest Code Generator.
import Foundation
import azureSwiftRuntime
internal struct ContainerSettingsData : ContainerSettingsProtocol {
    public var imageSourceRegistry: ImageSourceRegistryProtocol

        enum CodingKeys: String, CodingKey {case imageSourceRegistry = "imageSourceRegistry"
        }

  public init(imageSourceRegistry: ImageSourceRegistryProtocol)  {
    self.imageSourceRegistry = imageSourceRegistry
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)
      self.imageSourceRegistry = try container.decode(ImageSourceRegistryData.self, forKey: .imageSourceRegistry)
    if var pageDecoder = decoder as? PageDecoder  {
      if pageDecoder.isPagedData,
        let nextLinkName = pageDecoder.nextLinkName {
          pageDecoder.nextLink = try UnknownCodingKey.decodeStringForKey(decoder: decoder, keyForDecode: nextLinkName)
      }
    }
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)
    try container.encode(self.imageSourceRegistry as! ImageSourceRegistryData, forKey: .imageSourceRegistry)
  }
}

extension DataFactory {
  public static func createContainerSettingsProtocol(imageSourceRegistry: ImageSourceRegistryProtocol) -> ContainerSettingsProtocol {
    return ContainerSettingsData(imageSourceRegistry: imageSourceRegistry)
  }
}
