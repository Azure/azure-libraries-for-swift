// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License. See License.txt in the project root for license information.
//
// Code generated by Microsoft (R) AutoRest Code Generator.
import Foundation
import azureSwiftRuntime
internal struct AzureFileShareReferenceData : AzureFileShareReferenceProtocol {
    public var accountName: String
    public var azureFileUrl: String
    public var credentials: AzureStorageCredentialsInfoProtocol
    public var relativeMountPath: String
    public var fileMode: String?
    public var directoryMode: String?

        enum CodingKeys: String, CodingKey {case accountName = "accountName"
        case azureFileUrl = "azureFileUrl"
        case credentials = "credentials"
        case relativeMountPath = "relativeMountPath"
        case fileMode = "fileMode"
        case directoryMode = "directoryMode"
        }

  public init(accountName: String, azureFileUrl: String, credentials: AzureStorageCredentialsInfoProtocol, relativeMountPath: String)  {
    self.accountName = accountName
    self.azureFileUrl = azureFileUrl
    self.credentials = credentials
    self.relativeMountPath = relativeMountPath
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)
      self.accountName = try container.decode(String.self, forKey: .accountName)
    self.azureFileUrl = try container.decode(String.self, forKey: .azureFileUrl)
    self.credentials = try container.decode(AzureStorageCredentialsInfoData.self, forKey: .credentials)
    self.relativeMountPath = try container.decode(String.self, forKey: .relativeMountPath)
    if container.contains(.fileMode) {
        self.fileMode = try container.decode(String?.self, forKey: .fileMode)
    }
    if container.contains(.directoryMode) {
        self.directoryMode = try container.decode(String?.self, forKey: .directoryMode)
    }
    if var pageDecoder = decoder as? PageDecoder  {
      if pageDecoder.isPagedData,
        let nextLinkName = pageDecoder.nextLinkName {
          pageDecoder.nextLink = try UnknownCodingKey.decodeStringForKey(decoder: decoder, keyForDecode: nextLinkName)
      }
    }
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)
    try container.encode(self.accountName, forKey: .accountName)
    try container.encode(self.azureFileUrl, forKey: .azureFileUrl)
    try container.encode(self.credentials as! AzureStorageCredentialsInfoData, forKey: .credentials)
    try container.encode(self.relativeMountPath, forKey: .relativeMountPath)
    if self.fileMode != nil {try container.encode(self.fileMode, forKey: .fileMode)}
    if self.directoryMode != nil {try container.encode(self.directoryMode, forKey: .directoryMode)}
  }
}

extension DataFactory {
  public static func createAzureFileShareReferenceProtocol(accountName: String, azureFileUrl: String, credentials: AzureStorageCredentialsInfoProtocol, relativeMountPath: String) -> AzureFileShareReferenceProtocol {
    return AzureFileShareReferenceData(accountName: accountName, azureFileUrl: azureFileUrl, credentials: credentials, relativeMountPath: relativeMountPath)
  }
}
