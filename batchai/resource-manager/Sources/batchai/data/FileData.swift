// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License. See License.txt in the project root for license information.
//
// Code generated by Microsoft (R) AutoRest Code Generator.
import Foundation
import azureSwiftRuntime
internal struct FileData : FileProtocol {
    public var name: String
    public var downloadUrl: String
    public var properties: FilePropertiesProtocol?

        enum CodingKeys: String, CodingKey {case name = "name"
        case downloadUrl = "downloadUrl"
        case properties = "properties"
        }

  public init(name: String, downloadUrl: String)  {
    self.name = name
    self.downloadUrl = downloadUrl
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)
      self.name = try container.decode(String.self, forKey: .name)
    self.downloadUrl = try container.decode(String.self, forKey: .downloadUrl)
    if container.contains(.properties) {
        self.properties = try container.decode(FilePropertiesData?.self, forKey: .properties)
    }
    if var pageDecoder = decoder as? PageDecoder  {
      if pageDecoder.isPagedData,
        let nextLinkName = pageDecoder.nextLinkName {
          pageDecoder.nextLink = try UnknownCodingKey.decodeStringForKey(decoder: decoder, keyForDecode: nextLinkName)
      }
    }
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)
    try container.encode(self.name, forKey: .name)
    try container.encode(self.downloadUrl, forKey: .downloadUrl)
    if self.properties != nil {try container.encode(self.properties as! FilePropertiesData?, forKey: .properties)}
  }
}

extension DataFactory {
  public static func createFileProtocol(name: String, downloadUrl: String) -> FileProtocol {
    return FileData(name: name, downloadUrl: downloadUrl)
  }
}
