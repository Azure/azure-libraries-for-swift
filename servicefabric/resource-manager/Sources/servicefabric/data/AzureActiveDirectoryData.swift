// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License. See License.txt in the project root for license information.
//
// Code generated by Microsoft (R) AutoRest Code Generator.
import Foundation
import azureSwiftRuntime
internal struct AzureActiveDirectoryData : AzureActiveDirectoryProtocol {
    public var tenantId: String?
    public var clusterApplication: String?
    public var clientApplication: String?

        enum CodingKeys: String, CodingKey {case tenantId = "tenantId"
        case clusterApplication = "clusterApplication"
        case clientApplication = "clientApplication"
        }

  public init()  {
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)
      if container.contains(.tenantId) {
        self.tenantId = try container.decode(String?.self, forKey: .tenantId)
    }
    if container.contains(.clusterApplication) {
        self.clusterApplication = try container.decode(String?.self, forKey: .clusterApplication)
    }
    if container.contains(.clientApplication) {
        self.clientApplication = try container.decode(String?.self, forKey: .clientApplication)
    }
    if var pageDecoder = decoder as? PageDecoder  {
      if pageDecoder.isPagedData,
        let nextLinkName = pageDecoder.nextLinkName {
          pageDecoder.nextLink = try UnknownCodingKey.decodeStringForKey(decoder: decoder, keyForDecode: nextLinkName)
      }
    }
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)
    if self.tenantId != nil {try container.encode(self.tenantId, forKey: .tenantId)}
    if self.clusterApplication != nil {try container.encode(self.clusterApplication, forKey: .clusterApplication)}
    if self.clientApplication != nil {try container.encode(self.clientApplication, forKey: .clientApplication)}
  }
}

extension DataFactory {
  public static func createAzureActiveDirectoryProtocol() -> AzureActiveDirectoryProtocol {
    return AzureActiveDirectoryData()
  }
}
