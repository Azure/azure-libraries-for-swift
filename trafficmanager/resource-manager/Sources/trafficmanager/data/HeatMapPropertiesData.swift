// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License. See License.txt in the project root for license information.
//
// Code generated by Microsoft (R) AutoRest Code Generator.
import Foundation
import azureSwiftRuntime
internal struct HeatMapPropertiesData : HeatMapPropertiesProtocol {
    public var startTime: Date?
    public var endTime: Date?
    public var endpoints: [HeatMapEndpointProtocol?]?
    public var trafficFlows: [TrafficFlowProtocol?]?

        enum CodingKeys: String, CodingKey {case startTime = "startTime"
        case endTime = "endTime"
        case endpoints = "endpoints"
        case trafficFlows = "trafficFlows"
        }

  public init()  {
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)
      if container.contains(.startTime) {
        self.startTime = DateConverter.fromString(dateStr: (try container.decode(String?.self, forKey: .startTime)), format: .dateTime)
    }
    if container.contains(.endTime) {
        self.endTime = DateConverter.fromString(dateStr: (try container.decode(String?.self, forKey: .endTime)), format: .dateTime)
    }
    if container.contains(.endpoints) {
        self.endpoints = try container.decode([HeatMapEndpointData?]?.self, forKey: .endpoints)
    }
    if container.contains(.trafficFlows) {
        self.trafficFlows = try container.decode([TrafficFlowData?]?.self, forKey: .trafficFlows)
    }
    if var pageDecoder = decoder as? PageDecoder  {
      if pageDecoder.isPagedData,
        let nextLinkName = pageDecoder.nextLinkName {
          pageDecoder.nextLink = try UnknownCodingKey.decodeStringForKey(decoder: decoder, keyForDecode: nextLinkName)
      }
    }
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)
    if self.startTime != nil {
        try container.encode(DateConverter.toString(date: self.startTime!, format: .dateTime), forKey: .startTime)
    }
    if self.endTime != nil {
        try container.encode(DateConverter.toString(date: self.endTime!, format: .dateTime), forKey: .endTime)
    }
    if self.endpoints != nil {try container.encode(self.endpoints as! [HeatMapEndpointData?]?, forKey: .endpoints)}
    if self.trafficFlows != nil {try container.encode(self.trafficFlows as! [TrafficFlowData?]?, forKey: .trafficFlows)}
  }
}

extension DataFactory {
  public static func createHeatMapPropertiesProtocol() -> HeatMapPropertiesProtocol {
    return HeatMapPropertiesData()
  }
}
